# Версия приходит из CI (переменная GO_VERSION) — чтобы она задавалась в одном
# месте. Значение по умолчанию нужно для локальной сборки `docker build`.
ARG GO_VERSION=1.26.5
FROM golang:${GO_VERSION}-alpine

RUN apk add --no-cache gcc musl-dev git
